{
  "name": "mixamo-downloader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Resilient Playwright CLI for downloading Mixamo animations",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "scan": "node src/scan.mjs",
    "check": "node --check src/cli.mjs && node --check src/mixamo.mjs && node --check src/state.mjs && node --check src/scan.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "1.62.0"
  }
}
